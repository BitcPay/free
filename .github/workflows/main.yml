name: RDP Full Stack with Proxy Tools

on:
  workflow_dispatch:  # This enables the manual run button

jobs:
  setup-rdp:
    runs-on: windows-latest
    timeout-minutes: 3600

    steps:
      - name: Display Start Info
        run: |
          Write-Host "🚀 Starting RDP Full Stack Setup..."
          Write-Host "GitHub Run ID: $env:GITHUB_RUN_ID"

      - name: Configure RDP
        run: |
          # Enable RDP
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0
          
          # Disable NLA for easier connection
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0
          
          # Firewall rules
          netsh advfirewall firewall add rule name="RDP" dir=in action=allow protocol=TCP localport=3389
          
          # Restart service
          Restart-Service TermService -Force

      - name: Create RDP User
        run: |
          # Generate secure password
          Add-Type -AssemblyName System.Web
          $password = [System.Web.Security.Membership]::GeneratePassword(12, 2)
          $securePass = ConvertTo-SecureString $password -AsPlainText -Force
          
          # Save password to environment
          echo "RDP_PASSWORD=$password" >> $env:GITHUB_ENV
          
          # Create or update user
          $userName = "rdpuser"
          $userExists = Get-LocalUser -Name $userName -ErrorAction SilentlyContinue
          
          if ($userExists) {
              Set-LocalUser -Name $userName -Password $securePass
          } else {
              New-LocalUser -Name $userName -Password $securePass -AccountNeverExpires
              Add-LocalGroupMember -Group "Administrators" -Member $userName
              Add-LocalGroupMember -Group "Remote Desktop Users" -Member $userName
          }
          
          Write-Host "User created: $userName"
          Write-Host "Password: $password"

      - name: Install Essential Tools
        run: |
          # Install Chocolatey
          Set-ExecutionPolicy Bypass -Scope Process -Force
          [System.Net.ServicePointManager]::SecurityProtocol = [System.Net.ServicePointManager]::SecurityProtocol -bor 3072
          iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1'))
          
          # Install tools
          choco install -y googlechrome firefox vscode git python nodejs 7zip notepadplusplus

      - name: Install Tailscale
        run: |
          $tsUrl = "https://pkgs.tailscale.com/stable/tailscale-setup-latest-amd64.msi"
          $installer = "$env:TEMP\tailscale.msi"
          Invoke-WebRequest -Uri $tsUrl -OutFile $installer
          msiexec /i $installer /quiet /norestart
          Start-Sleep -Seconds 10
          
          # Connect Tailscale
          if ("${{ secrets.TAILSCALE_AUTH_KEY }}" -ne "") {
              & "C:\Program Files\Tailscale\tailscale.exe" up --authkey="${{ secrets.TAILSCALE_AUTH_KEY }}" --hostname=github-rdp-$env:GITHUB_RUN_ID
          } else {
              Write-Host "⚠️ No Tailscale auth key provided. Please add TAILSCALE_AUTH_KEY to secrets."
          }

      - name: Install XAMPP
        run: |
          Write-Host "Installing XAMPP..."
          $xamppUrl = "https://sourceforge.net/projects/xampp/files/XAMPP%20Windows/8.2.12/xampp-windows-x64-8.2.12-0-VS16-installer.exe/download"
          $installer = "$env:TEMP\xampp.exe"
          
          # Download
          Invoke-WebRequest -Uri $xamppUrl -OutFile $installer
          
          # Install silently
          Start-Process -FilePath $installer -ArgumentList "--mode unattended" -Wait
          
          # Create shortcut
          $shell = New-Object -ComObject WScript.Shell
          $shortcut = $shell.CreateShortcut("$env:USERPROFILE\Desktop\XAMPP.lnk")
          $shortcut.TargetPath = "C:\xampp\xampp-control.exe"
          $shortcut.Save()
          
          Write-Host "✓ XAMPP installed"

      - name: Install ProxyScrape Proxy Checker
        run: |
          Write-Host "Installing ProxyScrape Proxy Checker..."
          
          # Download
          $proxyCheckerUrl = "https://downloads.cdn.proxyscrape.com/proxy-checker/proxyscrape-proxy-checker-v1.1.0-x64-win-installer.exe"
          $installer = "$env:TEMP\proxyscrape-checker.exe"
          Invoke-WebRequest -Uri $proxyCheckerUrl -OutFile $installer
          
          # Install silently
          Start-Process -FilePath $installer -ArgumentList "/S" -Wait
          
          # Create shortcut
          $shell = New-Object -ComObject WScript.Shell
          $shortcut = $shell.CreateShortcut("$env:USERPROFILE\Desktop\ProxyScrape Proxy Checker.lnk")
          $shortcut.TargetPath = "C:\Program Files\ProxyScrape Proxy Checker\proxyscrape-proxy-checker.exe"
          $shortcut.Save()
          
          Write-Host "✓ ProxyScrape Proxy Checker installed"

      - name: Display Connection Info
        run: |
          # Get Tailscale IP if available
          $tsIP = ""
          if (Test-Path "C:\Program Files\Tailscale\tailscale.exe") {
              $tsIP = & "C:\Program Files\Tailscale\tailscale.exe" ip -4 2>$null
          }
          
          Write-Host ""
          Write-Host "══════════════════════════════════════════════════"
          Write-Host "           ✅ RDP SETUP COMPLETE"
          Write-Host "══════════════════════════════════════════════════"
          Write-Host ""
          Write-Host "📱 CONNECT WITH:"
          Write-Host "   Username: rdpuser"
          Write-Host "   Password: $env:RDP_PASSWORD"
          Write-Host ""
          
          if ($tsIP) {
              Write-Host "🔗 Tailscale IP: $tsIP"
              Write-Host "   Connect via: mstsc /v:$tsIP"
          } else {
              Write-Host "🔗 Connect to this machine's public IP"
          }
          
          Write-Host ""
          Write-Host "📦 INSTALLED SOFTWARE:"
          Write-Host "   • Chrome, Firefox, VS Code"
          Write-Host "   • Git, Python, Node.js"
          Write-Host "   • XAMPP (Apache + MySQL + PHP)"
          Write-Host "   • ProxyScrape Proxy Checker"
          Write-Host "   • Notepad++, 7-Zip"
          Write-Host ""
          Write-Host "💡 Shortcuts available on Desktop"
          Write-Host "══════════════════════════════════════════════════"
          
          # Keep the runner alive
          Write-Host ""
          Write-Host "⏰ RDP session will stay active for 60 minutes..."
          Write-Host "   Press Ctrl+C in runner or cancel workflow to stop."
          Write-Host ""
          
          # Keep-alive loop
          $startTime = Get-Date
          while ($true) {
              $elapsed = (Get-Date) - $startTime
              Write-Host "[$(Get-Date -Format 'HH:mm:ss')] Active for $([math]::Floor($elapsed.TotalMinutes)) minutes"
              Start-Sleep -Seconds 60
          }
